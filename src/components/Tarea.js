import React from "react";

export default function Tarea({ titulo, notas}) {
    return (
        <div>
            <div>
            </div>
            <div>
                <h1>{titulo}</h1>
                <p>{notas}</p>
            </div>
        </div>
    )
}